function ahSlider(sliderId,content,options={}){let defaultOptions={autoScroll:true,scrollTimeout:3e3,showNavBtn:true,showIndicators:true,prevLabel:"&#10094;",nextLabel:"&#10095;",indicatorsColor:"#4d4dff",roundedIndicators:false,polygon:false};let settings=Object.assign(defaultOptions,options);let slider=document.querySelector(`#${sliderId}`);for(let x=0;x<content.length;x++){const slide=document.createElement("div");slide.classList.add("ah-slider_item");const image=document.createElement("img");image.setAttribute("src",content[x].image);image.setAttribute("alt",`${x+1} slide`);slide.append(image);const title=document.createElement("div");title.classList.add("ah-slider_item__title");title.innerHTML=content[x].title;slide.append(title);const text=document.createElement("div");text.classList.add("ah-slider_item__text");text.innerHTML=content[x].text;slide.append(text);slider.append(slide)}const prev=document.createElement("span");prev.setAttribute("data-bs-slide","prev");prev.classList.add("ah-prev");prev.innerHTML=settings.prevLabel;slider.append(prev);const next=document.createElement("span");next.setAttribute("data-bs-slide","next");next.classList.add("ah-next");next.innerHTML=settings.nextLabel;slider.append(next);let innerSliders=document.querySelectorAll(`#${sliderId} .ah-slider_item`);const sliderDots=document.createElement("div");sliderDots.classList.add("ah-slider_dots");for(let y=0;y<innerSliders.length;y++){const dot=document.createElement("span");dot.setAttribute("data-bs-slide-to",(y+1).toString());dot.classList.add("ah-slider_dots__item");if(settings.roundedIndicators){dot.style.width="0.8rem";dot.style.height="0.8rem";dot.style.borderRadius="50%";dot.style.margin="0 0.4rem"}sliderDots.append(dot)}slider.append(sliderDots);let slideIndex=1;if(settings.polygon){let currentSlider=document.querySelector(`#${sliderId}`);currentSlider.style.backgroundColor=settings.indicatorsColor}showSlides(slideIndex,sliderId);function nextSlide(sliderId){showSlides(slideIndex+=1,sliderId)}function prevSlide(sliderId){showSlides(slideIndex-=1,sliderId)}function currentSlide(n,sliderId){showSlides(slideIndex=n,sliderId)}let timerId;if(settings.autoScroll){timerId=setInterval(()=>nextSlide(sliderId),settings.scrollTimeout)}const nextArrow=document.querySelector(`#${sliderId} [data-bs-slide="next"]`);nextArrow.addEventListener("click",function(){if(timerId){clearInterval(timerId);timerId=null}nextSlide(sliderId)});const prevArrow=document.querySelector(`#${sliderId} [data-bs-slide="prev"]`);prevArrow.addEventListener("click",function(){if(timerId){clearInterval(timerId);timerId=null}prevSlide(sliderId)});const dots=document.querySelectorAll(`#${sliderId} .ah-slider_dots__item`);dots.forEach(el=>{let attrTo=el.getAttribute("data-bs-slide-to");el.addEventListener("click",function(){if(timerId){clearInterval(timerId);timerId=null}currentSlide(attrTo,sliderId)})});if(!settings.showIndicators){for(let a=0;a<dots.length;a++){dots[a].style.display="none"}}if(!settings.showNavBtn){prevArrow.style.display="none";nextArrow.style.display="none"}function showSlides(n){let slides=document.querySelectorAll(`#${sliderId} .ah-slider_item`);let images=document.querySelectorAll(`#${sliderId} .ah-slider_item img`);let dots=document.querySelectorAll(`#${sliderId} [data-bs-slide-to]`);if(n>slides.length){slideIndex=1}if(n<1){slideIndex=slides.length}for(let i=0;i<slides.length;i++){slides[i].style.display="none"}slides[slideIndex-1].style.display="block";if(settings.polygon){images[slideIndex-1].style.clipPath="polygon(0% 10%, 30% 0%, 95% 0%, 100% 33%, 100% 90%, 70% 100%, 5%"+" 100%, 0% 66%)"}for(i=0;i<dots.length;i++){dots[i].style.backgroundColor="white"}dots[slideIndex-1].style.backgroundColor=settings.indicatorsColor}}